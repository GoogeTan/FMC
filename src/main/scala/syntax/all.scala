package fmc
package syntax

object all:
  export bimap.{*, given}
  export block.{*, given}
  export foldable.{*, given}
  export iron.{*, given}
  export monad.{*, given}
  export monoid.{*, given}
  export show.{*, given}
  export swap.{*, given}
end all
